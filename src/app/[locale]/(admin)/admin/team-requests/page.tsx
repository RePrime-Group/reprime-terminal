import TeamRequestsClient from '@/components/admin/TeamRequestsClient';

export const metadata = { title: 'Team Requests — RePrime Terminal Beta Admin' };

export default function TeamRequestsPage() {
  return <TeamRequestsClient />;
}
